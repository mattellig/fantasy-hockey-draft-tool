import * as React from 'react';
import * as ReactDOM from 'react-dom';
import './index.css';

ReactDOM.render(
    <React.StrictMode>
        <p>Hello, world!</p>
    </React.StrictMode>,
    document.getElementById('root')
);
